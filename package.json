{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev-server": "lerna run dev-server",
    "dev": "lerna run dev",
    "watch": "lerna run watch",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^6.6.1"
  }
}
